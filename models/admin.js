class Admin{

    constructor(pid, pname, pemail, ppassword){
        this.id = pid
        this.name = pname
        this.email = pemail
        this.password = ppassword
    }

}

module.exports = Admin